/* Single definition file for EditorStyle globals */
#include "EditorStyle.h"

namespace EditorStyle {
    // Single, external definition of FontScale to avoid duplicate-symbols across TUs
    float FontScale = 1.0f;
}
